#include <stdio.h>

int main() {
    float compra, total;

    printf("Monto de compra: ");
    scanf("%f", &compra);

    if (compra > 2500)
        total = compra * 0.92;  // aplica el 8% de descuento
    else
        total = compra;

    printf("Total a pagar: %.2f\n", total);

    return 0;
}
